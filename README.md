# LinkedIn Jobs Scraper (Selenium)

Powerful LinkedIn job scraper using Selenium – specially built for dynamic/JavaScript-heavy pages where BeautifulSoup alone fails.

## Features
- Handles infinite scroll & JavaScript rendering
- Extracts job title, company, location, salary (when available), job link
- Works on linkedin.com/jobs (global + filtered searches)
- Bypasses basic bot detection with proper delays
- Clean console output (easy to save in CSV/Excel)

## Requirements
```bash
pip install selenium
